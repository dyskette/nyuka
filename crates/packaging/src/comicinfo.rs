//! `ComicInfo.xml` v2.0 generation.
//!
//! Populate at minimum `Series`, `Number`, `Volume`, `Title`, `Summary`,
//! `Genre`, `LanguageISO`, `PageCount`, `Web`, `Year`/`Month`/`Day`, and
//! `Manga`. This is the metadata channel other readers actually use, so it is
//! the interoperability contract rather than an extra.
